create for me anime search app using react app and mui 
import React, { useState } from 'react'; import SearchBar from './SearchBar'; import AnimeList from './AnimeList'; interface Anime { mal_id: number; title: string; images: { jpg: { image_url: string; }; }; } const App: React.FC = () => { const [animeList, setAnimeList] = useState<Anime[]>([]); const searchAnime = async (query: string) => { const response = await fetch(https://api.jikan.moe/v4/anime?q=${query}&limit=10); const data = await response.json(); setAnimeList(data.data || []); }; return ( <div className="container"> <h1>Anime Search App</h1> <SearchBar onSearch={searchAnime} /> <AnimeList animes={animeList} /> </div> ); }; export default App; use material for this app search bar using debounce 250ms import React, { useState } from 'react'; interface SearchBarProps { onSearch: (query: string) => void; } const SearchBar: React.FC<SearchBarProps> = ({ onSearch }) => { const [query, setQuery] = useState(''); const handleSearch = () => { onSearch(query); }; return ( <div> <input type="text" value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search for an anime" /> <button onClick={handleSearch}>Search</button> </div> ); }; export default SearchBar;
useEffect(() => { const handler = setTimeout(() => { if (query.trim()) onSearch(query); }, 250); return () => { clearTimeout(handler); }; }, [query, onSearch]); got problem its looping
import React from "react"; import { Grid, Card, CardMedia, CardContent, Typography } from "@mui/material"; interface Anime { mal_id: number; title: string; images: { jpg: { image_url: string; }; }; } interface AnimeListProps { animes: Anime[]; } const AnimeList: React.FC<AnimeListProps> = ({ animes }) => { if (animes.length === 0) return ( <Typography align="center" color="text.secondary"> No results found. Try searching for something! </Typography> ); return ( <Grid container spacing={3}> {animes.map((anime) => ( <Grid size={{xs:12,sm:12,md:4}} key={anime.mal_id}> <Card sx={{ borderRadius: 3, boxShadow: 3 }}> <CardMedia component="img" height="300" image={anime.images.jpg.image_url} alt={anime.title} /> <CardContent> <Typography variant="subtitle1" fontWeight="bold" noWrap> {anime.title} </Typography> </CardContent> </Card> </Grid> ))} </Grid> ); }; export default AnimeList; show some information like this <div class="film-detail"> <h3 class="film-name"><a href="/beyblade-x-18632" class="dynamic-name" data-jname="Beyblade X" title="Beyblade X">Beyblade X</a></h3> <div class="fd-infor"> <span class="fdi-item">TV</span> <span class="dot"></span> <span class="fdi-item fdi-duration">22m</span> </div> </div>
entire app must be dark mode
export default AnimeList; dont the change the grid prop size={{}} always use this for future or next time when hover show rating, summary how many episode and seasons lastly show button play if clicked go to detail page
<motion.div initial={{ opacity: 0 }} whileHover={{ opacity: 1 }} transition={{ duration: 0.3 }} style={{ position: "absolute", top: 0, left: 0, width: "100%", height: "100%", background: "linear-gradient(to top, rgba(0,0,0,0.90), rgba(0,0,0,0.8))", color: "white", display: "flex", flexDirection: "column", justifyContent: "flex-end", padding: "16px", opacity: 0, }} > <Typography variant="body2" sx={{ mb: 0.5, fontWeight: "bold", color: "#fbc02d" }} > ⭐ {anime.score ? anime.score.toFixed(1) : "N/A"} </Typography> <Typography variant="body2" sx={{ maxHeight: "70px", overflow: "hidden", textOverflow: "ellipsis", }} > {anime.synopsis || "No synopsis available."} </Typography> <Typography variant="caption" sx={{ mt: 1, display: "block", color: "#b0b0b0" }} > {anime.episodes ? ${anime.episodes} Episodes : "Episodes: N/A"} </Typography> <Button variant="contained" size="small" color="primary" endIcon={<PlayArrowIcon />} href={anime.url} target="_blank" sx={{ mt: 1, alignSelf: "flex-start", borderRadius: 10 }} > Play </Button> </motion.div> how to fix the content too much space on top when hover
first show title, a bit space break line follow by rating episodes, rating and episode is grey and small text under that show synopsis or summarry with max 75 words if more than that the end of words show ...
how can i use redux for this app
after clicked detail page in animelist and go back to animelist after landed detailepage search bar still save the query
implement inifitescroll
implement skeleton loading